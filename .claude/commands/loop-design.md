# /loop-design
Kullanıcının amaç + başarı kriterlerinden tam loop tasarlar.
Akış: intent normalize → constraint discovery → template seç → contract oluştur →
baseline planla → task graph yaz (plan.md) → Gate A+B kontrolü → kullanıcıya özet.
Kritik eksik bilgi varsa TEK net soru sor; değilse varsayımı active-assumptions.md'ye yaz.
